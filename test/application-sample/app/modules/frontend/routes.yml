home:
    path: /
    defaults:
        _action: index


hello-world:
    path: /hello
    defaults:
        _action: hello


ping:
    path: /ping


errored:
    path: /errored


with-config:
    path: /with-config

customer:
    path: /customer/{id}


with-middleware:
    path: /with-middleware

with-view:
    path: /with-view
